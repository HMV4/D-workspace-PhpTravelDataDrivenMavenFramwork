package pageFactory_TestCases;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.testng.annotations.Test;

import pages.*;

public class quickBookTest {

	static WebDriver driver;

	@Test
	public static void quickBooking() {
		
		PHPlogin quickBookObject = new PHPlogin(driver);
		PHPlogin.loginPhp();
		System.out.println("login success");
		Quickbook bookObject= new Quickbook(driver);
		Quickbook.quickBookButtonClick();
		Quickbook.applyTax();
		Quickbook.service();
		Quickbook.nextButtonClick();

	}

}
