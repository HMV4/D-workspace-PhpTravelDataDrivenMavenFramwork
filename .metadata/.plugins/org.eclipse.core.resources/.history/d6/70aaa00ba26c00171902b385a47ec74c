package pageFactory_TestCases;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.testng.annotations.Test;
import pages.*;

import pages.Quickbook;


public class quickBookTest extends PHPlogin {

	
	public quickBookTest(WebDriver driver) {
		super(driver);
		// TODO Auto-generated constructor stub
	}

	@Test
	public void quickBooking()
	{
		WebDriver driver=new FirefoxDriver();
		driver.manage().window().maximize();
		
		//PHPlogin loginDriver= new quickBookTest(driver);
		PHPlogin.loginPhp();
		System.out.println("login success");
		//Quickbook quickBookObject= new Quickbook(driver);
		Quickbook.quickBookButtonClick();
		Quickbook.applyTax();
		Quickbook.service();
		Quickbook.nextButtonClick();
		
	}
	
	
	
}
